# Get External IP / local IPs
alias ip="curl ipinfo.io/ip"
alias ips="ifconfig -a | perl -nle'/(\d+\.\d+\.\d+\.\d+)/ && print $1'"
alias speedtest="wget -O /dev/null http://speedtest.wdc01.softlayer.com/downloads/test10.zip"

# Quickly serve the current directory as HTTP
alias serve='ruby -run -e httpd . -p 8000'  # Or python -m SimpleHTTPServer :)

alias kitt="open -a 'Google Chrome' https://kitt.lewagon.com/"

# Open tickets page
tickets() {
  open -a 'Google Chrome' https://kitt.lewagon.com/camps/$1/tickets
}

# Open calendar page
calendar() {
  open -a 'Google Chrome' https://kitt.lewagon.com/camps/$1/calendar
}

# Open webpage on chrome
website() {
  open -a 'google chrome' https://www.$1
}

# Open slack
alias slack="open -a 'slack'"

# Kick start day
alias work="rails s; open -a slack; open -a 'google chrome' http://localhost:3000; open -a 'google chrome' https://kitt.lewagon.com; open -a 'google chrome' https://calendar.google.com/calendar/b/1/r; open -a 'google chrome' https://github.com/lewagon/kitt; open -a mail; stt;"

# Open chrome
alias chrome="open -a 'google chrome'"

# Open an app
app() {
  open -a "$1"
}
